<h1 class="title">Blog index</h1>

<% @posts.each do |post| %>
  <div class="card mb-3">
    <div class="card-title">
      <%= link_to post.title, post %>
    </div>
    <div class="card-body">
      <%= post.content %>
    </div>
  </div>
<% end %>
